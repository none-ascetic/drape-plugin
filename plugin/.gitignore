# Credentials — never commit
mcp-server/.env
mcp-server/.env.local

# Brand context — written by /drape:setup, never commit
context/

# Dependencies
node_modules/

# Build output — dist/index.js is committed (pre-compiled bundle for Co-Work)
dist/*.map

# OS
.DS_Store
