# Credentials — never commit
mcp-server/.env
mcp-server/.env.local

# Brand context — written by /drape:setup, never commit
context/

# Dependencies
node_modules/

# Build output
dist/

# OS
.DS_Store
